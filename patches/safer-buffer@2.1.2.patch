diff --git a/dangerous.js b/dangerous.js
index ca41fdc549b6553e811d35e44730a51bec68be99..bc6eedb76b2d87b1edca4a99e5dcd4341ba86f90 100644
--- a/dangerous.js
+++ b/dangerous.js
@@ -12,7 +12,7 @@ var dangerous = {}
 var key
 
 for (key in safer) {
-  if (!safer.hasOwnProperty(key)) continue
+  if (!Object.prototype.hasOwnProperty.call(safer, key)) continue
   dangerous[key] = safer[key]
 }
 
@@ -20,14 +20,14 @@ var Dangereous = dangerous.Buffer = {}
 
 // Copy Safer API
 for (key in Safer) {
-  if (!Safer.hasOwnProperty(key)) continue
+  if (!Object.prototype.hasOwnProperty.call(Safer, key)) continue
   Dangereous[key] = Safer[key]
 }
 
 // Copy those missing unsafe methods, if they are present
 for (key in Buffer) {
-  if (!Buffer.hasOwnProperty(key)) continue
-  if (Dangereous.hasOwnProperty(key)) continue
+  if (!Object.prototype.hasOwnProperty.call(Buffer, key)) continue
+  if (Object.prototype.hasOwnProperty.call(Dangereous, key)) continue
   Dangereous[key] = Buffer[key]
 }
 
diff --git a/safer.js b/safer.js
index 37c7e1aa6cbd4effd94ee28bd7b0655756b80cea..c7c9a9c49723db649255ba2477856b956d109ffd 100644
--- a/safer.js
+++ b/safer.js
@@ -10,14 +10,14 @@ var safer = {}
 var key
 
 for (key in buffer) {
-  if (!buffer.hasOwnProperty(key)) continue
+  if (!Object.prototype.hasOwnProperty.call(buffer, key)) continue
   if (key === 'SlowBuffer' || key === 'Buffer') continue
   safer[key] = buffer[key]
 }
 
 var Safer = safer.Buffer = {}
 for (key in Buffer) {
-  if (!Buffer.hasOwnProperty(key)) continue
+  if (!Object.prototype.hasOwnProperty.call(Buffer, key)) continue
   if (key === 'allocUnsafe' || key === 'allocUnsafeSlow') continue
   Safer[key] = Buffer[key]
 }
